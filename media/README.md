# Media

Directory layout:

- `source/`: downloaded upstream media, kept out of Git when large
- `blank/`: derived blank or neutral base maps
- `locator/`: per-DDD locator maps
- `raster/`: optional exported PNGs for Anki if SVG performance is not acceptable

The original Wikimedia SVG is intentionally downloaded via script instead of committed directly because it is very large.

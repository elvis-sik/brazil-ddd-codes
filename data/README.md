# Data

This directory holds small, auditable source manifests and seed tables used to build the deck.

Current plan:

- keep the original map provenance in CSV form
- keep official municipality-to-CN provenance in CSV form
- add a canonical `DDD` reference table once the source map structure is understood
- avoid burying critical reference data inside scripts
